<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Incidents</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="shortcut icon" href="/img/favicon.ico" type="image/x-icon">
    <!-- BOOTSTRAP ICONS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
</head>
<body>
    <nav id="navbar">
        <a href="/" id="logo">
        <img src="/img/icone_incident.png" alt="Incidents logo">
        </a>
        {{#if session.userid}}
        <p>{{user.name}}</p>
        {{/if}}
        <ul>
            <li><a href="/">Todos Incidentes</a></li>
            {{#if session.userid}}
                <li><a href="/incidents/userIncidents">Meus incidentes</a></li>
                <li><a href="/incidents/report">Reportar</a></li>
                <li><a href="/incidents/dashboard">Dashboard</a></li>
                <li><a href="/logout">Sair</a></li>
            {{else}}
                <li><a href="/login">Entrar</a></li>
                <li><a href="/register">Registrar</a></li>
            {{/if}}
        </ul>
    </nav>
    <div class="container">
        <!-- Mensagens -->
        {{#if messages.message}}
        <div class="message">
            {{messages.message}}
        </div>
        {{/if}}
        {{{ body }}}
    </div>
    <footer>
        <p><span>Incidents</span> &copy; 2024</p>
    </footer>
</body>
</html>